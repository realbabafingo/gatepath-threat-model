# Tor Configuration Examples

This directory contains example Tor client configurations
used for the optional Tor SOCKS gateway.

These examples:
- run Tor in client-only mode
- do not configure relays or exits
- are intended for documentation purposes only

No secrets or operational values are included.
